services:
  - type: web
    name: cartify-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5002
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
